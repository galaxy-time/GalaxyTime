{
  "name": "gt-server",
  "description": "galaxy time server",
  "version": "1.0.0",
  "packageManager": "yarn@4.0.2",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn install && yarn workspace api dev",
    "build": "yarn workspaces foreach --topological-dev -pi run build",
    "docker": "docker build . -t gt-server:latest && docker run -it -p 3000:3000 gt-server:local yarn workspace hello-api start "
  }
}
